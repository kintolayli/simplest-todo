# ─── Пользовательские данные (личные настройки плагина) ───────────────────
# НЕ коммитить — у каждого пользователя свои пути и настройки
data.json

# ─── Node.js (для будущего перехода на TypeScript/esbuild) ────────────────
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# ─── Сборка (TypeScript → main.js) ────────────────────────────────────────
# Раскомментировать когда перейдёшь на TypeScript + esbuild
# main.js
# *.js.map

# ─── macOS ────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
._*

# ─── Windows ──────────────────────────────────────────────────────────────
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ─── IDE и редакторы ──────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ─── TypeScript (когда перейдёшь) ─────────────────────────────────────────
*.tsbuildinfo
